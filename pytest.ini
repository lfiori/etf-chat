[pytest]
testpaths = tests
asyncio_mode = auto
addopts = -v --tb=short -m "not quality and not e2e"
markers =
    quality: Test di qualità delle risposte chat (richiedono ANTHROPIC_API_KEY, lenti)
    e2e: Test end-to-end Playwright (richiedono server in esecuzione)
